<div class="modal fade" id="addCryptoPayment" tabindex="-1" role="dialog"
    data-backdrop="static">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title t-primary">
                    Cryptocurrency Payment
                </h5>
                <button type="button" class="close" data-dismiss="modal"
                    aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form action="/web3/payment/crypto" method="post">
                    <div class="form-group">
                        <label for="saleId">Sale ID</label>
                        <input type="text"
                            class="form-control"
                            name="saleId" id="crypto-sale-id"
                            placeholder="Select a sale" required readonly/>
                    </div>
                    <div class="form-group">
                        <label for="walletAddress">Customer Wallet Address</label>
                        <input type="text" class="form-control"
                            name="walletAddress"
                            placeholder="0x..."
                            pattern="^0x[a-fA-F0-9]{40}$"
                            title="Enter a valid Ethereum address starting with 0x"
                            required />
                    </div>
                    <div class="row align-items-center">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="amount">Amount</label>
                                <input type="number" class="form-control"
                                    name="amount" step="0.000001"
                                    placeholder="0.01"
                                    required min="0"/>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="currency">Currency</label>
                                <select class="form-control" name="currency" required>
                                    <option value="ETH">Ethereum (ETH)</option>
                                    <option value="BTC">Bitcoin (BTC)</option>
                                    <option value="USDT">Tether (USDT)</option>
                                    <option value="USDC">USD Coin (USDC)</option>
                                </select>
                            </div>
                        </div>
                    </div>
                    <div class="alert alert-info">
                        <small>
                            <i class="ic-info-circle"></i> 
                            The fiat equivalent will be calculated based on current exchange rates.
                            <br>
                            <strong>Note:</strong> In a production environment, this would connect to a wallet like MetaMask.
                        </small>
                    </div>

                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary"
                            data-dismiss="modal">
                            <i class="ic-cross"></i> Close
                        </button>
                        <button type="submit" class="btn btn-primary">
                            <i class="ic-check"></i> Process Payment
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>